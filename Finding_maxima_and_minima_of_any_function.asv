clc;

syms x;
y = inline ('2*x^3 + 3*x^2 -12*x +17', 'x');
%grid on;
ezplot(y , [-3,2]);
grid on;
g = diff(y(x), (x));
s = solve (g)

syms F;
k = inline ('(()*F^2) / ( (10*F^2)^2 + (F^2)*(F^2 - 1)*((10-1)^2)*0.5^2 )^2');
%grid on;
ezplot(y , [-3,2]);
grid on;
g = diff(y(x), (x));
s = solve (g)
